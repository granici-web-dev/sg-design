# Microsoft Clarity project id (Clarity dashboard → Settings → Overview).
# Not a secret: it ships in the client bundle by design, so it lives in git
# and every environment (local, CI, hosting) picks it up without extra setup.
NEXT_PUBLIC_CLARITY_PROJECT_ID=xxpsiovk87

# Google Analytics 4 measurement id (GA4 → Admin → Data Streams).
# Public by design as well — it ships in the page for every visitor.
NEXT_PUBLIC_GA_ID=G-RFX819R0ZY
